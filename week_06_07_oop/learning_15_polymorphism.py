# Polymorphism
# An example of polymorphism is the len function
# it can handle counting the length of either strings or lists
# it knows how to handle different kinds of objects

name = 'Jim'
print(len(name))

list = ['one','two']
print(len(list))